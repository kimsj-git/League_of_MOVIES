server {
    listen 3100;
    location / {
        root    /app;
        index   index.html;
        try_files $uri $uri/ /index.html;
    }
    location /images/ {
        root    /usr/share;
    }
}